# Зависимости разработки. Ставятся поверх requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Линтер и форматировщик
ruff>=0.8

# Проверка аннотаций типов (в CLAUDE.md они обязательны)
mypy>=1.13

# Тесты
pytest>=8.0
pytest-asyncio>=0.24
